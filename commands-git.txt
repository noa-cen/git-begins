# Cloner un repository:
git clone lien_du_repository

# Les trois commandes push:
git add nom_du_fichier.extension
git add . (permet d'indexer tous les fichiers du repository)
git commit -m "message"
git push

# La commande qui permet de pull:
git pull

# Commande permettant de créer une branche:
git branch nom_de_la_nouvelle_branche

# Commande permettant de basculer de branche:
git checkout nom_de_la_nouvelle_branche
